# terminal-sandbox
a node.js service that starts to make telnet fun again

# Application
```
node server.js
```

# Statistics
- configurable motd
- configurable port to connect (currently 4096)
- Can add commands if needed
- supports thousands of simulaneous connections
